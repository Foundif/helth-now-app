# Helth Now App

Create me a app like this concept first complete all UI I need a working full app prototype app name Helth use the app logo i attached what I need was full working prototype first create all UI it's a simple app don't take too much time and don't waste credits

The app uses Supabase for database, authentication, storage, and server-side data access.

## Development

You need Node.js and npm.

For AI summaries, configure `GEMINI_API_KEY` in the server environment. The app uses
`gemini-2.5-flash` by default; set `GEMINI_MODEL` to override it. Keep the key server-side
and configure it in Vercel for Production, Preview, and Development environments.

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```
